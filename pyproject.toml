[project]
name = "srm-website"
version = "0.1.0"
description = "SRM team website tooling for converting CAD references into web-ready point cloud assets."
requires-python = ">=3.10"
dependencies = [
  "pillow>=10.0.0",
]

[tool.uv]
package = false
